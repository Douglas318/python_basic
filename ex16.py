from math import cos, sin, tan 
angulo = float(input('informe o ângulo: '))
seno = sin(angulo)
cosseno = cos(angulo)
tangente = tan(angulo)

print('Os valores de seno, cosseno e tangente são respectivamente: {}, {} e {}'.format(seno, cosseno, tangente))